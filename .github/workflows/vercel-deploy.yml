name: Vercel Production Deployment

on:
  push:
    branches:
      - master

# GitHub Actions runs all jobs in parallel by default
jobs:
  # JOB 1: Deploys the Patient PWA
  deploy-pwa:
    runs-on: ubuntu-latest
    # These env variables are available to all steps in THIS job
    env:
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID_PWA }}
      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
      
    steps:
      - name: Check out source code
        uses: actions/checkout@v3

      - name: Install Vercel CLI
        run: npm install -g vercel@latest

      - name: Pull Vercel Environment Information
        run: vercel pull --yes --environment=production --token=$VERCEL_TOKEN --cwd ./patient-pwa

      - name: Build Project Artifacts
        run: vercel build --prod --token=$VERCEL_TOKEN --cwd ./patient-pwa

      - name: Deploy Project Artifacts to Vercel
        run: vercel deploy --prebuilt --prod --token=$VERCEL_TOKEN --cwd ./patient-pwa

  # JOB 2: Deploys the Clinic Dashboard
  deploy-dashboard:
    runs-on: ubuntu-latest
    # These env variables are available to all steps in THIS job
    env:
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID_DASHBOARD }}
      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
      
    steps:
      - name: Check out source code
        uses: actions/checkout@v3

      - name: Install Vercel CLI
        run: npm install -g vercel@latest

      - name: Pull Vercel Environment Information
        run: vercel pull --yes --environment=production --token=$VERCEL_TOKEN --cwd ./clinic-dashboard

      - name: Build Project Artifacts
        run: vercel build --prod --token=$VERCEL_TOKEN --cwd ./clinic-dashboard

      - name: Deploy Project Artifacts to Vercel
        run: vercel deploy --prebuilt --prod --token=$VERCEL_TOKEN --cwd ./clinic-dashboard
