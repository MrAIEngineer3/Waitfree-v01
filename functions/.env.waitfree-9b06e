# Non-secret production configuration
PATIENT_PWA_BASE_URL=https://app.waitfreeclinic.com
# Comma-separated list of allowed origins for CORS
CORS_ALLOWED_ORIGINS=https://waitfreeclinic.com,https://app.waitfreeclinic.com

# Sensitive values such as Twilio credentials should be stored in `.env.waitfree-9b06e.local`
# (ignored by git) or provisioned via Secret Manager. Example keys:
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_WHATSAPP_FROM=
